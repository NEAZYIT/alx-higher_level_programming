// Trigger color change on clicking the element with id 'red_header'
$('#red_header').on('click', function () {
    // Update the text color of the <header> element to red
    $('header').css('color', '#FF0000');
});
