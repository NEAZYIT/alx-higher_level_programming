-- Calculate Score Average
-- This script calculates the average score of all records in the "second_table" of the "hbtn_0c_0" database.

-- Calculate the average score.
SELECT AVG(score) AS average
FROM second_table;
